namespace JetDatabaseWriter.Schema.Expressions;

internal enum CalculatedFunctionDomain
{
    Logical = 0,
    Text = 1,
    DateTime = 2,
    Numeric = 3,
    Formatting = 4,
    Financial = 5,
    Metadata = 6,
}
